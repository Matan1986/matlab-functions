# Template: PLAN

**Context (required):** Read `docs/repo_context_minimal.md` first. Skip bulk docs in `docs/agent_prompt_exclude.md`.

## Plan sections

1. Objective (one paragraph)
2. Files touched (paths only)
3. Parallelism: confirm outputs are run-scoped; no shared repo-root writes
4. Observability: what will be logged (`agent_runs_log` / `agent_warnings_log`)

# Mode coupling analysis (Agent E)

**Question:** Are `kappa1` and `alpha` (and `kappa2`) dynamically/structurally coupled, and does `alpha_res` depend on `kappa1` once PT (`spread90_50`) is in the model?

- **alpha / residual / PT:** `C:/Dev/matlab-functions/tables/alpha_from_PT.csv`
- **kappa1 / kappa2 / alpha (structure table):** `C:/Dev/matlab-functions/tables/alpha_structure.csv`

## Correlations (pairwise complete)

| pair | Pearson | Spearman | n |
|---|---:|---:|---:|
| kappa1 vs alpha | 0.0169571 | -0.525275 | 14 |
| kappa2 vs alpha | 0.961536 | 0.951648 | 14 |
| alpha_res vs kappa1 | -0.212887 | -0.356643 | 12 |

## Regression: `alpha_res` ~ `kappa1` + PT

LOOCV OLS on rows with finite `residual_best`, `kappa1`, `spread90_50` (n = 12).

| model | LOOCV RMSE | Pearson(y,ŷ) | Spearman(y,ŷ) |
|---|---:|---:|---:|
| alpha_res ~ PT (spread90_50) | 0.555861 | 0.464611 | 0.363636 |
| alpha_res ~ kappa1 | 0.504522 | 0.212887 | 0.356643 |
| alpha_res ~ kappa1 + PT (spread90_50) | 0.685443 | 0.473308 | 0.629371 |

## Verdict rule (documented)

- **Static coupling:** |Pearson| or |Spearman| for **kappa1 vs alpha** ≥ 0.35 with n ≥ 4.
- **Residual link:** same threshold for **alpha_res vs kappa1**, *or* LOOCV RMSE for `alpha_res ~ kappa1 + PT` **strictly below** `alpha_res ~ PT` only.
- **YES:** static coupling *and* (residual link *or* strict LOOCV improvement).
- **PARTIAL:** one branch holds but not both.
- **NO:** neither branch.

## **MODE_COUPLING_PRESENT: YES**

## Interpretation note

On n = 12, **LOOCV RMSE increases** when `kappa1` is added alongside `spread90_50` (joint model worse than PT-only). That does **not** contradict coupling: `kappa1` alone predicts `alpha_res` better than PT alone, and rank correlations show structure. The joint OLS is unstable when both predictors encode overlapping geometry.

## Artifacts

- `C:/Dev/matlab-functions/tables/mode_coupling_agent_e.csv`

*Auto-generated by `analysis/run_mode_coupling_agent_e.m`.*

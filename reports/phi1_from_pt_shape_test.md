# Phi1 From PT Alone (Shape-Level Test)

## Goal
Test whether the canonical `Phi1(x)` *shape* can be generated as a constrained, mechanistic functional of the barrier landscape `PT` (via `CDF(PT)`), without any amplitude-only fitting and without a large basis search.

## Strict PT-derived basis
Computed on the canonical `xGrid` after mapping `x=(I-I_{peak}(T))/w(T)` using stored scaling parameters.

| term | definition |
|---|---|
| `CDF_PT_x` | `CDF(P_T)(x)` |
| `d_dx_CDF_PT_x` | `d/dx CDF(P_T)(x)` |
| `d2_dx2_CDF_PT_x` | `d^2/dx^2 CDF(P_T)(x)` (smooth-then-differentiate) |
| `x_times_d_dx_CDF_PT_x` | `x * d/dx CDF(P_T)(x)` |
| `x2_times_d2_dx2_CDF_PT_x` | `x^2 * d^2/dx^2 CDF(P_T)(x)` |
| `smoothed_curvature_like_x` | `movmean(d^2/dx^2 CDF(P_T), 9)` |

## Combination policy
1-term candidates were scored individually.
Then every 2-term PT-only model of the form `a*term_i + b*term_j` was scored, with `(a,b)` obtained by constrained least-squares *on the strict chosen pair* (no search over the coefficients, no ML).

## Metrics and robustness
- Cosine similarity and Pearson correlation use the finite common mask; cosine is zero-mean unit-L2.
- RMSE is after best scalar rescaling: `min_alpha ||Phi1 - alpha*psi||_2`.
- Even/odd uses reflection on `xGrid` and compares even-energy fractions: `even_fraction_match = |evenFrac(Phi1)-evenFrac(psi)|`.
- Robustness split: main uses `canonical_T_le_30K_including_22K`, robustness uses `canonical_T_le_30K_excluding_22K`.

## Best PT-only model (by lowest normalized RMSE)

- Model: `CDF_PT_x + smoothed_curvature_like_x` (n_terms=2)
- Main: cosine=0.799172, Pearson=0.799172, RMSE=0.187765, even_fraction_match=0.361849, RMSE_norm=0.477779
- Excl 22K: cosine=0.771270, Pearson=0.771270, RMSE=0.198768, even_fraction_match=0.367724, RMSE_norm=0.505779

## Baseline (canonical Phi1 shape)

- Phi1 vs Phi1 under the same finite-mask metric definitions is exact: cosine=1, Pearson=1, RMSE=0, even_fraction_match=0.
- This is the shape-level gold standard; PT-only models are judged by how close they get to this baseline on both RMSE and parity under the 22K robustness split.

## Final verdicts (required format)

PHI1_FROM_PT_ALONE_SUPPORTED: PARTIAL
PHI1_REQUIRES_INDEPENDENT_COLLECTIVE_MODE: PARTIAL

## Interpretation (plain language)

PT-derived differential/curvature-weighted descriptors capture a nontrivial part of Phi1’s symmetry/curvature structure, but the best PT-only combination still falls short of the canonical Phi1 shape quality (especially in normalized RMSE and/or robustness). This supports a “partial geometric resemblance” view: Phi1’s shape partly tracks barrier/CDF operators, but not fully reducible to PT alone.

*Auto-generated by `tools/run_phi1_from_pt_shape_test.m`.*
